# Vulcan

AI harness with Docker workspace, terminal, and Etna MCP integration.

## Install

```
etna install vulcan
```

## Distribution

Vulcan is distributed via [vulcan-versions](https://github.com/dwhite-sys/vulcan-versions).
Releases are built automatically on `git tag v<version> && git push origin v<version>`.
